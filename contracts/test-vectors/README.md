# Model Test Vectors

Each promoted model bundle must name versioned input and expected-output fixtures here. The candidate example points to this policy file because no model has passed promotion yet.

Do not add fabricated audio or expected scores. Add a fixture only after its source, license, preprocessing, and expected tolerance are reviewed.
